media:
  input: src/assets/blog
  output: "../../assets/blog"

content:
  - name: blog
    label: Blog
    type: collection
    path: src/content/blog
    view:
      fields: [title, pubDate, author, isPublished]
      primary: title
      sort: [pubDate]
      default: { sort: pubDate, order: desc }
    fields:
      - { label: "Title", name: "title", type: "string", required: true }
      - { label: "Description", name: "description", type: "text", required: true }
      - { label: "Date", name: "pubDate", type: "date", required: true, options: { time: true, format: "yyyy-MM-dd'T'HH:mm:ss" } }
      - { label: "Author", name: "author", type: "select", required: true, options: { values: ["John Lilly", "Kara Lilly"] } }
      - { label: "Published", name: "isPublished", type: "boolean", default: false }
      - { label: "Hero Image", name: "heroImage", type: "image" }
      - { label: "Gallery Photos", name: "galleryPhotos", type: "image", list: true }
      - { label: "Body", name: "body", type: "rich-text" }

settings:
  content:
    merge: true
